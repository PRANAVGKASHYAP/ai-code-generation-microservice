# k8s/services/intelligence-service.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: intellegence-service
  namespace: ai-apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: intellegence-service
  template:
    metadata:
      labels:
        app: intellegence-service
    spec:
      containers:
        - name: intellegence-service
          image: pranavgk2003/llm_apps_intellegence-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 9030
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: k8s
            - name: CONFIG_SERVER_URL
              value: http://config-service.ai-apps.svc.cluster.local:8888
            - name: SPRING_CLOUD_CONFIG_FAIL_FAST
              value: "false"
            - name: SPRING_CLOUD_CONFIG_RETRY_MAX_ATTEMPTS
              value: "10"
            - name: SPRING_CLOUD_CONFIG_RETRY_INITIAL_INTERVAL
              value: "3000"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DB_PASSWORD
            - name: LLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: LLM_API_KEY
            - name: JWT_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: JWT_SECRET
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 250m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /intellegence/actuator/health
              port: 9030
            initialDelaySeconds: 90
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /intellegence/actuator/health
              port: 9030
            failureThreshold: 30
            periodSeconds: 10
            timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: intellegence-service
  namespace: ai-apps
spec:
  selector:
    app: intellegence-service
  ports:
    - port: 80
      targetPort: 9030